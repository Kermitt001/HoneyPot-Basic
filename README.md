# HoneyPot-Basic
Low-interaction honeypot for lab/CTF use: HTTP decoy endpoints + SSH-like TCP banner, safe fake login/shell, and detailed JSON/SQLite logging. Detects common patterns (SQLi, XSS, traversal, brute force, scanners) with simple CLI stats &amp; Docker support.
